server:
  port: 8080

spring:
  application:
    name: silsonfit-api

  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %msg%n"